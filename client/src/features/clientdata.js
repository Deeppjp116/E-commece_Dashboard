import { createSlice } from '@reduxjs/toolkit';

export const clientSclice = createSlice({
  name: 'client',
  initialState: {},
  reducers: {},
});

export const {} = clientSclice.actions;

export default clientSclice.reducer;
